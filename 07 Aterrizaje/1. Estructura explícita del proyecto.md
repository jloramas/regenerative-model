# Estructura explícita del proyecto

Este documento define las **estructuras mínimas necesarias** para que un proyecto basado en generación y regeneración asistida por IA sea **operable, coherente y verificable** desde el primer día.

No describe cómo actúa la IA (eso pertenece al Documento II), sino **sobre qué puede actuar**, **qué existe en el proyecto** y **qué significado tiene cada cosa**.

El objetivo es simple:

crear un terreno suficientemente estructurado para que la IA pueda operar con precisión, sin introducir rigidez innecesaria ni fricción en la fase exploratoria.

---

## 1\. Principio fundamental

En este modelo, **nada es un artefacto por accidente**.

Un artefacto:

* no se descubre,  
* no se infiere,  
* no se deduce del contenido de un fichero.

👉 **Un artefacto existe porque está explícitamente declarado como tal.**

Esto reduce ambigüedad, coste cognitivo y exploración innecesaria por parte de la IA y de los humanos.

---

## 2\. Qué es un artefacto (en términos operativos)

Un **artefacto** es una unidad de trabajo declarada que agrupa, de forma autocontenida:

* **Intención** (qué es / para qué existe)  
* **Realización** (código, assets, estructura)  
* **Verificación** (criterios, pruebas, checks)

Un artefacto puede representar:

* un elemento UI,  
* una página,  
* una sección compleja,  
* una herramienta interna,  
* o cualquier otra unidad significativa del sistema.

Lo importante no es su forma, sino que:

* tenga identidad,  
* tenga límites,  
* y pueda ser operado como una unidad.

---

## 3\. Declaración explícita centralizada de artefactos

Todos los artefactos del proyecto se declaran en un **registro central**, equivalente conceptual a un `package.json` o `workspace manifest`.

Ejemplo conceptual (no normativo):

```
artifacts:
  - id: ui.button.primary
    path: app/ui/button-primary
    cycle: exploratorio

  - id: page.patients
    path: app/pages/patients
    cycle: exploratorio

  - id: catalog.master-detail
    path: catalog/layouts/master-detail
    cycle: exploratorio
```

Este registro es la **fuente de verdad**.

La IA **no necesita escanear el sistema de ficheros** para descubrir artefactos. Opera únicamente sobre:

* artefactos declarados,  
* y otros elementos cuando un workflow lo indique explícitamente.

---

## 4\. Forma de un artefacto

Un artefacto puede adoptar distintas formas físicas:

### 4.1 Artefacto simple (un solo fichero)

Válido cuando:

* la complejidad es baja,  
* la intención es clara,  
* no requiere descomposición.

Ejemplo:

```
app/ui/empty-state.tsx
```

La metadata **no vive dentro del fichero**. Vive en el registro central de artefactos.

---

### 4.2 Artefacto compuesto (directorio)

Cuando la complejidad crece, un artefacto puede evolucionar a directorio:

```
app/pages/patients/
  index.tsx
  spec.md
  tests.md
  components/
```

Esto **no cambia su identidad**: sigue siendo el mismo artefacto, con la misma declaración.

---

## 5\. Estructura del proyecto (mínima)

El modelo no impone una estructura rígida, pero sí distingue **zonas semánticas** habituales:

```
app/        → artefactos de aplicación
catalog/   → artefactos de referencia / reutilizables
specs/     → requisitos transversales
ai/        → reglas y workflows IA
```

⚠️ Importante: un artefacto **puede vivir en cualquier parte del árbol**, siempre que esté declarado.

---

## 6\. Artefactos y jerarquía de UI

En la parte de aplicación, los artefactos **pueden anidarse** siguiendo la estructura de navegación y composición de la UI:

```
app/
  layout/
    patients/
      tabs/
        clinical/
        financial/
```

Cada nivel puede:

* ser o no un artefacto,  
* según se declare explícitamente.

Esto permite:

* reflejar la estructura mental del producto,  
* facilitar regeneración localizada,  
* y evitar monolitos difíciles de operar.

---

## 7\. Relación entre artefactos

Un artefacto puede:

* inspirarse en otros,  
* componerse a partir de otros,  
* depender semánticamente de otros.

Estas relaciones **se declaran**, no se infieren.

La relación **no implica reutilización de código**, sino:

* reutilización de intención,  
* de estructura,  
* o de reglas.

La generación sigue siendo **end-to-end**.

---

## 8\. Qué no es un artefacto

No todo en el proyecto es un artefacto.

Ejemplos típicos que **no lo son por defecto**:

* scripts de tooling,  
* configuración de build,  
* documentación general,  
* pruebas técnicas de bajo nivel.

Estos elementos:

* pueden tener reglas propias,  
* pero no participan del ciclo regenerativo salvo que se declaren explícitamente.

---

## 9\. Chequeos de integridad estructural

El proyecto puede disponer de checks automáticos para detectar:

* artefactos declarados sin contenido,  
* contenido significativo no declarado como artefacto,  
* rutas inconsistentes,  
* referencias a artefactos inexistentes.

Estos checks **no bloquean la exploración**, pero generan señales tempranas.

---

## 10\. Preparando el terreno para la IA

Este documento **no define cómo actúa la IA**, pero establece algo clave:

Los workflows IA **operan sobre artefactos declarados**, y aplican reglas específicas según el tipo de elemento sobre el que actúan.

Las reglas concretas de operación IA:

* viven en el sistema de reglas del proyecto,  
* se detallan en el Documento II,  
* y no están embebidas en la estructura.

---

## 11\. Cierre

Estas estructuras no buscan optimizar el presente, sino **hacer viable el futuro**:

* permitir exploración rápida sin caos,  
* permitir regeneración sin pérdida de intención,  
* y permitir que la IA actúe con disciplina, no por inferencia.

Con este terreno definido, el siguiente paso es natural:

👉 **definir el rol operativo de la IA sobre estas estructuras**.  
